name: Cluster Keywords (Full)

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (embed + cluster + label only, no DB writes)'
        type: boolean
        default: false
      k:
        description: 'Number of keyword groups (blank = script default, 200)'
        required: false

jobs:
  cluster-keywords:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install
      - run: npm run cluster-keywords
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          VOYAGE_API_KEY: ${{ secrets.VOYAGE_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
          K: ${{ github.event.inputs.k }}
